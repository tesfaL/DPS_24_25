spring.application.name=DESM-Administration-Server
server.port=8080

# Disable Spring Boot's default banner (we print our own)
spring.main.banner-mode=off

# Logging — keep it minimal so plant logs are readable
logging.level.root=WARN
logging.level.desm=INFO
